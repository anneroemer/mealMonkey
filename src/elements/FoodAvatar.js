import styled from "styled-components";

export default styled.img`
    height: 70px;
    width: 70px;
    border-radius: 50%;
    position: absolute;
    left: -1.5rem;
    box-shadow: 0px 3px 12px rgb(0,0,0,0.16);
`; 

//background: ${ props => props.img ? `url$({props.img})` : gray};
//border-radius: ${ props. => props.square ? "1,35em" : "50%"};